export const LOGIN = 'LOGIN'
export const LOGOUT = 'LOGOUT'

export const login = () => {
    return {
        type: LOGIN
    }
}

export const logout = () => {
    return {
        type: LOGIN
    }
}
